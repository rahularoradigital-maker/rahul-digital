# AdScale — Scale Plan: 1,000 daily users at 100% capacity

**Date:** 2026-09-04 · **Scope:** the whole codebase, every folder · **Status:** plan + flaw register (no code changed in this pass)

> **Method (what "check each line" meant here):** reading 46k lines by eye is unreliable. Instead every one of the 523 TS/TSX files in every folder was swept *mechanically* for each class of scale flaw — unbounded DB reads, tenant-scoping gaps, per-instance state, serial fan-out, ungated AI, unbounded parallelism, missing rate limits, function-duration assumptions — plus the live index coverage and the Meta call volume per sync. Every finding below carries a `file:line`. Where a first-pass flag turned out to be a false positive, it was re-checked and says so.

---

## 1. What "1,000 users at 100% capacity" actually means (load model)

| Dimension | Today (measured) | @1,000 DAU, full use | Multiplier |
|---|---|---|---|
| Users / connected accounts | 1 / 5 | 1,000 / ~1,500 (agencies hold several) | 300× |
| Active ads in the store | ~1,018 | ~90,000 (avg ~60/account) | 90× |
| `ad_metrics` rows (90-day window) | 14,588 | **~2.5–4M** (sparse spend-days) | ~200× |
| Nightly Meta Graph calls | ~15/account → ~75 | **~15–22k** (10–15 calls/account: ads list + insights per 40-ad chunk + meta) | 300× |
| Cockpit page views | a handful | ~5–10k/day (all served from L2 cache / rollups when warm) | — |
| AI calls (chat, creative, decode) | few | **thousands/day — the dominant variable cost** | — |
| Concurrent sync work | 1 account chain | 1,500 account jobs must finish inside the nightly window | — |

**Where the ceilings actually are (in order):** (1) the **Vercel Hobby plan** (60s functions, 2 daily crons, no commercial use), (2) **PostgREST's silent 1,000-row cap** on un-paged reads, (3) **one-request fan-out loops** that can't finish in a function timeout, (4) a **global (not per-tenant) AI budget**, (5) **per-instance** rate limiting/caching on serverless, (6) **no billing/self-serve approval/deletion executor** (business blockers, not technical ones).

The data tier is **not** the ceiling: Postgres handles 4M rows trivially, and the hot indexes are already right (see §5). The scoring engine is deterministic and cached. **This app scales to 1,000 DAU with the plan below and no rewrite.**

---

## 2. Folder-by-folder review (every folder, verdict)

| Folder | Files | Scale verdict | Notes |
|---|---|---|---|
| `app/api/cron/*` | 3 | 🔴 **must change** | daily-only cron + un-paged `ad_accounts` list (§4-A1) + one-request fan-out (§4-E1). Needs Pro + the durable queue. |
| `app/api/ingest/run` | 1 | 🟠 | resumable slice (good) but no per-user rate limit (§4-H); 300s assumed (§4-C). |
| `app/api/admin/rollups` | 1 | 🔴 | serial loop over ALL accounts in one request (§4-E1) — times out past ~100 accounts. |
| `app/api/{ask,creative-production/generate,creative/deep-analysis,competitors/analyze}` | 4 | 🟠 | expensive AI/Meta routes with **no rate limit** (§4-H); product-gated only. |
| `app/api/health` | 1 | 🟢 | admin-gated aggregate reads (unscoped by design). |
| `app/api/*` (other ~48 routes) | | 🟢 | product-gated (47 methods / 42 routes, `check:access-gate`); reads are user-scoped. |
| `app/app/*` (16 pages) | | 🟡 | 7 pages assume `maxDuration=300` (§4-C). Cockpit/funnel/changes read from cache/rollups — fine when warm. |
| `components/` (123) | | 🟢 | 72 client islands; heaviest are lane-owned (`studio.tsx` 1001, `competitor-dashboard` 457). Bundle 706 KB. Not a 1k-DAU ceiling. |
| `lib/ingest/` (4) | | 🟡 | resumable, chunked (AD_CHUNK=40), deadline-bounded — correct design; serial per chunk by intent. |
| `lib/meta-source.ts` (1,146 LOC) | | 🟡 | has 429/500/503 backoff + jitter (good); 12 serial awaits are cursor paging (inherent). Split is maintainability, not scale. |
| `lib/meta-sync.ts` (750) | | 🟡 | L1 `LruMap(500)` is per-instance (§4-B) — harmless (L2 `cockpit_cache` is the real shared layer). SWR 24h. |
| `lib/rollups/` (6) | | 🟢 | precompute-on-sync + self-heal; the correct pattern for scale. Boundary-guarded. |
| `lib/scoring/` (20), `lib/cockpit/` (8) | | 🟢 | deterministic, golden-guarded, cached. `event-roi-store.ts` has 2 un-paged `ad_meta` reads (§4-A2). |
| `lib/creative/`, `lib/creative-os/`, `lib/creative-production/` (57) | | 🟠 | 5 un-paged per-account reads (§4-A2); `orchestrator.ts` unbounded `Promise.all` AI fan-out (§4-G). |
| `lib/ai/` (13) | | 🟠 | router gates kill-switch + budget (good) but the budget is **global** (§4-F). |
| `lib/rate-limit.ts` / `-distributed.ts` | | 🟠 | in-process Map (§4-B); a **distributed variant already exists** — needs a KV backend + adoption. |
| `lib/tenancy/`, `lib/security/`, `lib/supabase/` | | 🟢 | app-level tenancy is consistently scoped (sweep found 0 real gaps — 2 flags were false positives). |
| `lib/influencer/` (25), `lib/growth/` (12), `lib/competitors/` (5) | | 🟡 | a few un-paged per-user reads (§4-A2); otherwise fine. |
| `lib/jobs/`, `lib/queue-postgres.ts` | | 🟢 **use it** | a durable queue (`claim_jobs`, visibility timeout, dead-letter) already exists and is under-used. It is the answer to §4-E. |
| `lib/billing/meter.ts`, `token_usage` | | 🟠 | per-tenant token metering exists; **Stripe not wired** — a business blocker at paid tiers. |
| `lib/notifications/`, `lib/observability` | | 🟡 | `captureError` seam exists; **no Sentry DSN, no RUM** yet. |
| `lib/rules/`, root `lib/{decision,validator,confidence}.ts` (14) | | ⚪ | v1 engine, unreachable from the app; entangled with checks. Not a scale item. |
| `scripts/` (183) | | 🟢 | 168 checks run in ~2s via `npm run check`; CI-enforced. |
| `supabase/migrations/` (50) | | 🟢 | forward-only, guarded (`check:migrations`); hot-table indexes correct (§5). |
| `docs/` (170) | | 🟡 | sprawl; `docs/README.md` is now the index. |

---

## 3. The verdict: what breaks first at 1,000 DAU (ranked)

1. **🔴 The nightly sync silently skips every account past #1,000.** `app/api/cron/sync/route.ts:98`, `cron/rollups/route.ts:22`, `admin/rollups/route.ts:14` list `ad_accounts` with a bare `.select()` — PostgREST returns **at most 1,000 rows** and no error. At ~1,500 accounts, ~500 tenants would never sync, with no alarm. *This is the single most important finding.*
2. **🔴 Vercel Hobby.** 15 files assume `maxDuration = 300`; Hobby caps at **60s** (Pro: 300s+). Hobby also allows only daily crons and forbids commercial use. The sync survives today only because it is resumable; at scale it needs 5× more hops. **Pro is a prerequisite, not an optimization.**
3. **🔴 One-request fan-out.** `cron/rollups` (4 workers) and `admin/rollups` (serial) process **all** accounts inside one function call: 1,500 accounts × ~3s ≈ 19 min ≫ 300s. They will time out.
4. **🟠 Silent truncation on per-account reads.** Un-paged reads of `ad_meta`/`ad_metrics`/`creative_semantics` return ≤1,000 rows for big (agency) accounts → **wrong numbers, no error** (`event-roi-store.ts:16,65`, `creative/deep-analysis.ts:46,53`, `creative/decode.ts:47`, `creative-os/store.ts:36,89`, `influencer/store.ts:26`).
5. **🟠 Global AI budget = noisy neighbor.** `aiBudgetExceeded()` has no tenant; one heavy user pauses AI for all 1,000.
6. **🟠 Per-instance rate limiting.** `lib/rate-limit.ts:13` is an in-process `Map`; on serverless every instance has its own counters, so limits don't hold. The expensive routes (`ask`, `generate`, `deep-analysis`, `competitors/analyze`, `ingest/run`) have **no** rate limit at all.
7. **🟠 Business blockers.** No Stripe/metering enforcement, manual approval panel, no account-deletion executor, no email provider, no Sentry/alerts wired. These stop a *launch*, not the servers.

---

## 4. Flaw register — evidence, fix, better way

**A. Unbounded reads (PostgREST silently caps at 1,000 rows)**
- **A1 — CRITICAL, tenant-skipping:** `ad_accounts` listed bare in `app/api/cron/sync/route.ts:98`, `app/api/cron/rollups/route.ts:22`, `app/api/admin/rollups/route.ts:14`.
  *Fix:* page with the existing `readAllPages` (ordered by `id`). *Better way:* the cron should not list accounts at all — it should **enqueue one job per account** into the durable queue (§E) and let the drain process them; the enqueue itself is paged.
- **A2 — HIGH, wrong numbers for big accounts:** `lib/scoring/event-roi-store.ts:16,65` (`ad_meta`), `lib/creative/deep-analysis.ts:46,53` (`ad_meta`, `ad_metrics`), `lib/creative/decode.ts:47` (`creative_semantics`), `lib/creative-os/store.ts:36,89`, `lib/influencer/store.ts:26`, `lib/competitors/data.ts:85`.
  *Fix:* `readAllPages` with a total order, or an explicit `.limit()` + "showing top N" where truncation is intended. *Better way:* a lint (`check:paged-reads`) that fails any `.select()` on a table that can exceed 1,000 rows/tenant without `.range()/.limit()/.maybeSingle()` — the same class of guard as `check:rollup-boundary`.
- **A3 — fine:** `ad_sync_state`, `system_flags`, `provider_keys`, `ai_usage`, `growth_*`, `org_members/orgs/brands` are small by nature (health/admin/aggregate). `tenancy/resolve.ts` becomes a concern only for agencies with >1,000 brands.

**B. Per-instance state on serverless**
- `lib/rate-limit.ts:13` in-process `Map` — ineffective across instances. **A distributed limiter already exists** (`lib/rate-limit-distributed.ts`, with an in-proc fallback) — it needs a shared KV backend (Vercel KV / Upstash) and to be the one routes import.
- `lib/meta-sync.ts:571` L1 `LruMap(500)` — per-instance; acceptable because L2 `cockpit_cache` (DB) is shared. *Better way:* at scale, move L1 to KV so a warm view is warm everywhere, and keep the 24h SWR.
- `lib/keys.ts:18` provider-key cache — fine (short-lived, small).

**C. `maxDuration = 300` assumed in 15 files** (Hobby caps 60s): `app/app/{page,action-center,changes,creative-production,creative,media,market}`, `api/{ingest/run,admin/rollups,creative-production/shopify/sync,creative-production/generate,creative/deep-analysis,jobs/drain,cron/rollups,cron/sync}`.
  *Fix:* Vercel **Pro** (and set `maxDuration` intentionally per route). *Better way:* nothing user-facing should need 300s — long work belongs in the queue (§E); pages should stream (`<Suspense>`).

**D. Tenancy scoping** — sweep result: **0 real gaps** across all `.from(<tenant table>)` reads. The two flags were false positives (`health` = admin aggregate by design; `oauth-store.ts:11` is a keyed *write*, and `readToken(accountId, userId)` is user-scoped). The model (RLS default-deny + service-role + `.eq("user_id")`) holds. *Better way:* the full store-layer lint (Phase 5 of the master audit) so this stays true as 15 sessions add code.

**E. One-request fan-out / serial loops**
- **E1 — WILL time out:** `cron/rollups` (bounded 4 workers, but all accounts in one request) and `admin/rollups` (serial). At 1,500 accounts ≈ 19 min.
  *Fix / better way:* **use the durable queue that already exists** (`lib/queue-postgres.ts`, `claim_jobs`, `/api/jobs/drain`): the cron enqueues one `sync-account` / `rollup-account` job per account; a drain runs every few minutes (Pro allows), claims a bounded batch with a visibility timeout, retries with backoff, dead-letters failures. Each account becomes independently resumable and observable (`jobs` table). This also replaces the fragile `after()`-kicked self-chain in `cron/sync`.
- E2 — by design, keep: `ingest/ad-metrics.ts` chunked serial with a deadline; `meta-source.ts` cursor paging; `change-history.ts` incremental.

**F. AI cost control**
- Gate exists and is enforced at the source: `lib/ai/router.ts:25-26,44` (`isKilled("ai")` + `aiBudgetExceeded()`); OpenAI chat goes only through the router. ✅
- **Flaw:** `aiBudgetExceeded()` is **global** (no tenant). *Fix:* debit **per-tenant** tokens at the router (`lib/billing/meter.ts` / `token_usage` already exist for pricing) and refuse when a tenant's monthly allowance is spent; keep the global cap as the circuit breaker. *Better way:* per-tenant + per-feature quotas (chat / creative-gen / vision), surfaced in the usage meter.

**G. Unbounded parallel fan-out:** `lib/agents/creative/orchestrator.ts` (×2), `lib/growth/draft.ts`, `creative-production/providers/google-gemini.ts` — `Promise.all(items.map(aiCall))` over user-sized arrays. *Fix:* bounded concurrency (the worker-pool pattern `cron/sync` already uses), so one "generate 40 variants" request can't fire 40 simultaneous AI calls.

**H. Missing rate limits on expensive routes:** `api/ask`, `api/creative-production/generate`, `api/creative/deep-analysis`, `api/competitors/analyze`, `api/ingest/run` — product-gated but unlimited. *Fix:* per-user limits via the distributed limiter (e.g. `ask` 30/min, `generate` 10/min, `ingest/run` 2/10min). `leads` and `avatar` already have them.

---

## 5. Data tier — verified live, mostly ready

Indexes on the hot tables are **correct for the app's filters**: `ad_metrics` (pkey `user,acct,ad,date`; `user,acct,ad`; `user,acct,date`), `ad_meta` (`user,acct`), `ad_changes` (`user,acct,date`; `user,acct,object`), rollups (pkey + `user,acct`), `cockpit_cache` (pkey), `notifications` (user/created, unread partial, dedupe unique), `jobs` (pending/claimed partials). The RLS init-plan fix (`0040`) is applied.
- **At 1k DAU (~4M rows):** fine as-is. **Add:** a retention policy (keep 13 months hot), and plan `ad_metrics` **partitioning by month at ~20M rows** (not now). Enable **PITR** (Supabase Pro). Drop the ~15 advisor-flagged unused indexes only after real traffic confirms they stay unused.
- Supabase-js goes through PostgREST (HTTP), so Postgres connection exhaustion is **not** the risk; the 1,000-row cap (§4-A) is.

---

## 6. Target architecture for 1,000 DAU (evolve, no rewrite)

```
Vercel PRO (300s fns, frequent crons)          Supabase PRO (PITR, compute)
 ├─ RSC pages: read rollups / cockpit_cache /   ├─ Postgres: ad_metrics (indexed, later partitioned)
 │  unstable_cache; stream cold paths            │  rollups, verifications, jobs (durable queue)
 ├─ API routes: product-gated + per-user         ├─ RLS default-deny + service-role (app-level tenancy,
 │  DISTRIBUTED rate limits (KV)                 │  lint-enforced store boundary)
 ├─ /api/cron/sync (every 15 min): ENQUEUE       Vercel KV / Upstash: shared L1 cache + rate-limit counters
 │  one job per account (paged)                  Meta Graph: 10–15 calls/account/night, backoff, bounded conc.
 └─ /api/jobs/drain (every 2–5 min): claim N,    Gemini/OpenAI: router-gated, PER-TENANT tokens + global breaker
    sync → rollups → verify, retry, dead-letter  Sentry (via captureError) + web-vitals RUM + alert webhook
```
**Keep:** Next.js RSC, Supabase, the deterministic engines, rollups + self-proving, the lean 13-dep tree. **Add:** Pro tiers, KV, queue-driven ingestion, per-tenant AI quotas, distributed limits, observability, billing/approval/deletion.

---

## 7. Phased plan

| Phase | Objective | Concrete work | Risk | Effort | Success metric | Rollback |
|---|---|---|---|---|---|---|
| **S0 Correctness that already bites** (do first, this week) | remove silent truncation | page the 3 `ad_accounts` lists (A1) + the 8 per-account reads (A2) with `readAllPages`; add `check:paged-reads` lint | LOW | 1–2 d | lint green; a 1,500-account fixture syncs all 1,500 | additive |
| **S1 Platform prerequisites** | remove hard ceilings | Vercel **Pro**; Supabase **Pro** (+PITR); set `maxDuration` per route intentionally; Sentry DSN into `captureError`; `ALERT_WEBHOOK_URL`; leaked-password toggle | LOW (owner actions) | 1 d | health `automationStale=false` daily; errors visible in Sentry | plan downgrade |
| **S2 Queue-driven ingestion** | finish 1,500 accounts nightly, observably | cron **enqueues** per-account jobs; drain every 2–5 min claims a bounded batch; sync→rollups→verify per job; retry/backoff/dead-letter; Meta 429 respected; replace `after()` self-chain | MED (money path; golden + ingest checks as contract) | 1 wk | 100% accounts synced by 06:00; `jobs` dead-letter = 0; `/api/health` shows coverage | keep old chain behind a flag one cycle |
| **S3 Shared state** | limits + cache hold across instances | KV backend for `rate-limit-distributed`; adopt on the 5 expensive routes (H); move cockpit L1 to KV | LOW | 2–3 d | limits enforced under a 2-instance load test; cache hit-rate ↑ | env-flag |
| **S4 Per-tenant AI economics** | no noisy neighbor, priced usage | router debits `token_usage` per tenant + per feature; refuse over allowance; keep global breaker; bounded concurrency in orchestrator/draft (G) | LOW–MED | 3–4 d | $/tenant/day tracked; one whale can't pause others | flag |
| **S5 Business readiness** | launch-able | Stripe checkout + webhook → plan → allowance; self-serve or bulk approval; account-deletion **executor** (manifest exists); email provider for the digest | MED (billing = stop-condition, owner approval) | 1–2 wk | signup→pay→use without an admin; deletion purges per manifest | Stripe test mode |
| **S6 Read-path polish** | instant at scale | `<Suspense>` streaming on the cockpit cold path; RUM (`web-vitals` → endpoint) to *measure* LCP/INP; keep rollups warm via the queue | MED (golden-adjacent; output-identical) | 3–4 d | cockpit p95 warm < 1.5s, cold < 5s; CWV measured | flag |
| **S7 Load test + launch gate** | prove it | synthetic 1,000-user / 1,500-account fixture; measure sync completion, p95s, Meta-limit headroom, DB CPU, $/day; chaos: kill a drain mid-run, force Meta 429 storm | LOW | 3 d | all metrics inside targets for 3 consecutive nights | — |

**Rule (unchanged from the master audit):** one phase, verified, documented, then the next; S2 and S6 touch the money path — done alone, `check:golden` + `check:shadow-benchmark` + `check:cockpit` byte-identical before merge.

---

## 8. Cost model @1,000 DAU (order of magnitude — validate in S7)

| Item | Estimate | Note |
|---|---|---|
| Vercel Pro | ~$20/seat + usage (functions/bandwidth) | low hundreds $/mo at this traffic |
| Supabase Pro | $25 + compute (small→medium instance) | ~4M rows is small |
| KV / Upstash | ~$0–20 | tiny |
| Meta Graph | $0 | rate-limited, not billed |
| **AI (Gemini/OpenAI)** | **dominant & variable** — driven by creative-gen + chat | per-tenant quotas (S4) are what make the plan solvent; price tiers must cover marginal $/tenant |
| Sentry / email | ~$26 + provider | |

**Infra is not the constraint — AI margin per tenant is.** S4 (per-tenant metering) is the economic linchpin.

---

## 9. What is already right (don't touch)
Deterministic, golden-guarded scoring · rollups + self-proving verify (precompute-on-sync is the correct scale pattern) · resumable chunked ingestion with backoff · correct hot-table indexes · app-level tenancy consistently scoped (0 real gaps) · a durable queue already built · 168 checks in ~2s, CI-enforced · lean dependency tree · AI gated at the router.

**Bottom line:** no rewrite. Fix the silent 1,000-row cap (S0), move to Pro (S1), drive ingestion through the queue you already built (S2), make limits/cache shared (S3), meter AI per tenant (S4), wire billing/approval/deletion (S5), then prove it with a load test (S7). That is a 1,000-DAU product.

---

## S0 — EXECUTED 2026-09-04 (remove silent truncation)

The single highest-priority correctness bug, fixed. All un-paged reads on large-per-tenant tables now page or bound.
- **A1 (the tenant-skipper):** the 3 `ad_accounts` lists (`cron/sync`, `cron/rollups`, `admin/rollups`) now use `readAllPages` (ordered), so the nightly sync can never silently skip accounts past #1,000.
- **A2:** `event-roi-store.ts` (2 `ad_meta` reads → paged; feeds the cockpit event-ROI), `influencer/store.ts` (2 reads → paged), `scope/events` (`.limit(5000)` moved inline).
- **False positives re-verified & dismissed:** `creative-os/store` (inserts), `competitors/data` (`.limit(1)`), `deep-analysis`/`decode` (`.in(ids)`-bounded), `meta-sync` (`.limit(1).maybeSingle()`).
- **Prevention:** `scripts/check-paged-reads.ts` (`check:paged-reads`, wired into `check:all`) — a statement-aware, comment-stripping lint that fails any unbounded `.select()` on the 6 large-per-tenant tables. It **caught a 4th `ad_accounts` truncation I'd missed by hand** during development, proving its worth.

**Verified:** `check:paged-reads` PASS (0 unbounded), 168/168 gate green, `check:golden` intact (scoring unchanged), tsc 0, build compiles. No product behavior changed for today's data volumes; the fix only matters once a table exceeds 1,000 rows/tenant.

---

## S2 (slice) — EXECUTED 2026-09-04 (queue-driven rollups)

The safe, self-contained first slice of queue-driven ingestion: the store-only rollup refresher (no Meta, no golden risk).
- **`/api/cron/rollups`** no longer refreshes every account INLINE (a one-request fan-out that times out past ~100 accounts). It now **batch-enqueues one durable `rollup-account` job per account** (paged list from S0) and kicks the drain. Fast + resumable.
- **`rollup-account` handler** (`lib/jobs/handlers.ts`) does the actual `refreshAccountRollup` + `refreshCreativeRollup`; idempotent, so a re-run/duplicate is harmless.
- **`/api/jobs/drain` self-chains**: a full batch kicks another drain, so one initial kick drains the whole backlog (e.g. 1,500 accounts) without a scheduled tick. Terminates when a drain claims < BATCH; the queue's retry→dead-letter prevents a stuck-job loop.
- **`check:rollup-wiring`** updated: cron/rollups must enqueue `rollup-account`, and the handler must call both refreshes.

**Verified:** tsc 0; `check:rollup-wiring` / `check:rollup-boundary` / `check:paged-reads` PASS; 168/168 gate green; `check:golden` intact (scoring untouched); build compiles; the batch-enqueue shape validated against the LIVE `jobs` schema (a test insert landed `pending`, then was deleted).

**Better than before:** the old inline fan-out lost all progress on a timeout; the queue version is resumable (jobs persist) and per-account observable in `jobs`. **Depends on the drain running** — the after()-kick + self-chain covers a single trigger; at scale add `/api/jobs/drain` to `vercel.json` as a periodic sweep (S1/Pro). **Same pattern applies next to `cron/sync`** (the remaining golden-path fan-out) — deferred, as `cron/sync` is the golden Meta path.

- **`/api/admin/rollups` converted too (2026-09-04):** the admin "refresh all rollups" button had the same one-request inline fan-out. It now batch-enqueues `rollup-account` jobs (same paged list + `after()`-kick pattern as cron/rollups) and returns immediately with the enqueued count. `check:rollup-wiring` was NOT tightened for admin (it is an operator convenience, not a scale-critical path), but it rides the same handler + drain.

---

## S3 — EXECUTED 2026-09-04 (per-user rate limits on the 5 expensive routes)

Adopt the distributed limiter (`lib/rate-limit-distributed.ts` — Upstash Redis REST when configured, in-proc `Map` fallback otherwise) on the routes that fan out to a paid provider or a full Meta sync, so one signed-in user (or a script on a valid session) cannot loop them and blow the bill or starve others. Each guard sits right after the auth check and returns `429` with a `Retry-After` header when limited.
- **`/api/ask`** — `ask:<uid>`, 30/min (Gemini answer + cockpit read).
- **`/api/creative-production/generate`** — `cp-generate:<uid>`, 15/min (image generation; the cost driver).
- **`/api/creative/deep-analysis`** (POST only) — `deep-analysis:<uid>`, 3/min (downloads + vision-decodes up to 10 videos).
- **`/api/competitors/analyze`** — `competitors-analyze:<uid>`, 10/min (Meta + AI fan-out).
- **`/api/ingest/run`** — `ingest-run:<uid>`, 3 per 10 min (a full Meta sync: many Graph calls + writes).

These are **short-window burst caps** layered on TOP of the existing economic caps (Ask's rolling-24h `reserve_ask_quota`, competitors' `DAILY_CREATIVE_CAP`, token metering) — the daily caps bound total spend, the per-minute caps bound burst/abuse. Nothing about the golden scoring path changed (these are request guards, not scoring logic).

**Verified:** tsc 0; 168/168 gate green; `check:golden` intact (scoring untouched); build compiles.

**Owner action to make it distributed:** the limiter is correct today (in-proc fallback works per-instance), but on Vercel serverless each instance has its own `Map`, so the real cap is per-instance until Upstash is wired. Add `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` env vars (S1/Pro) and `upstashConfigured()` flips it to a true cross-instance cap with **no code change**. Same shared-KV backend also serves the read-cache in S3's cache half (not yet built).

---

## S1 — OWNER ACTION (the gating prerequisite for S2/S3 at scale)
Not code I can ship — these are account/env/billing changes only Rahul can make:
1. **Vercel Pro** — lifts the 60s function cap (heavy syncs) and the 2-cron limit, and allows adding `/api/jobs/drain` to `vercel.json` as a scheduled sweep so the queue drains on a timer, not only on an `after()`-kick.
2. **Upstash Redis** (free tier is fine to start) — set `UPSTASH_REDIS_REST_URL` + `UPSTASH_REDIS_REST_TOKEN` in Vercel; flips S3's rate limits (and the future read-cache) from per-instance to true cross-instance with no code change.
3. **Pro tiers / billing** — the token allowances are coded; Stripe wiring + the leaked-password toggle remain owner tasks (S5).

---

## S4 — EXECUTED 2026-09-04 (per-tenant AI economics: the noisy-neighbour guard)

The economic linchpin, and the one real gap in an otherwise-complete AI-cost stack. What already existed and is correct (not rebuilt): every provider call's real token count + USD is recorded per user+task into `ai_usage` (`lib/ai/spend.ts`); a GLOBAL daily cost breaker pauses the router over `AI_DAILY_COST_BUDGET` (`lib/ai/budget.ts`); the product token meter debits `token_usage` atomically per user (`lib/billing/meter.ts`); the admin console reads per-user/provider/task spend.

**The gap (audit F-PERF-03):** the daily cost breaker was GLOBAL only. One heavy tenant crossing it made the router return `null` for **every** user - an AI outage for all, caused by one whale. The plan's success metric is literally "one whale can't pause others".

**The fix (root-cause, one place):** a per-TENANT daily ceiling checked in the router - the single choke point every AI task flows through, so it covers Ask, competitor vision fan-out, deep analysis, concept-gen, everything, with no per-caller edits.
- `tenantAiBudgetExceeded(userId)` (`lib/ai/budget.ts`) sums today's `ai_usage.cost_usd` for that user vs `AI_TENANT_DAILY_COST_BUDGET` (default **$5**, deliberately BELOW the global $25 default so no one tenant can eat the whole budget). Same 60s per-user cache + **fail-open** contract as the global check (a metering hiccup never blocks a paying user).
- The router (`runTaskText` + `runTaskJson`) checks the tenant ceiling right after the global one; an unattributed background call (userId null) skips it and stays bounded by the global cap.
- A whale now trips its OWN cap and gets degraded (callers already handle a `null` AI reply); everyone else keeps working.

**Verified:** `check:ai-budget` extended (asserts the tenant resolver AND that the tenant default sits below the global default) - PASS; `check:ai` router graph GREEN; tsc 0; 168/168 gate green; `check:golden` PASS (scoring path untouched); build compiles.

**Owner note:** the ceiling is enforced per serverless instance's cache but reads the shared `ai_usage` table, so the actual spend total is already global-accurate; the only thing Upstash (S1) would add here is cross-instance cache sharing to cut a few duplicate SELECTs - not correctness. Set `AI_TENANT_DAILY_COST_BUDGET` in Vercel to tune the per-tenant cap (or leave unset for the $5 default).

---

## S2 (final slice) — EXECUTED 2026-09-04 (cron/sync -> durable queue, the golden Meta path)

The last one-request/one-chain fan-out, converted - and the #3-ranked scale risk's remaining half. cron/sync was already resumable + self-chaining via `after()`, but the chain is fragile: a dropped hop costs a FULL DAY (Hobby fires the cron only daily), and there is no per-account retry/backoff/dead-letter or observability. The daily-mode warm loop (`fetchLiveCockpit` per active user, concurrency 3) is itself an in-request fan-out that would exceed 300s past ~100 users.

- **New `sync-account` job handler** (`lib/jobs/handlers.ts`): does exactly what the cron continue-hop does - resolve the account's token, run one bounded `syncAdMetrics` slice + change-history, and on completion refresh both rollups + log the drift verdict. If a slice is incomplete AND made progress, it **re-enqueues itself** (durable equivalent of the self-chain), capped at `SYNC_MAX_HOPS` (30) so a stuck account can't loop. A thrown error lets the queue retry with backoff, then dead-letter - which the old chain could never do.
- **cron/sync daily mode, opt-in behind `SYNC_VIA_QUEUE=1`**: enqueues one `sync-account` job per connected account (paged list from S0, chunked insert) + kicks the drain once (self-chains through the backlog). **Default OFF** - the proven self-chain path is byte-for-byte untouched until the owner flips the flag. The per-account WORK is identical, so golden/shadow numbers do not move.
- **`check:rollup-wiring`** extended: cron/sync must enqueue `sync-account`, and the handler must run `syncAdMetrics` + `verifyAndLog` - so the durable-sync path can't be silently dropped.

**Verified:** tsc 0; `check:rollup-wiring` PASS; `check:golden` + `check:shadow-benchmark` PASS (money path byte-identical); 168/168 gate green; build compiles; the `jobs` insert shape is identical to the S2 rollup-account enqueue already validated live.

**Honest limits (not yet 100%):** (1) I could NOT trigger a live overnight run from here, so the end-to-end drained sync of a real account is unproven live - though the handler mirrors the continue-hop already running in production. **Rahul must set `SYNC_VIA_QUEUE=1` and confirm one nightly run before this is trusted.** (2) With only the `after()`-kick drain (Hobby), the FINAL partial batch's continuation jobs can wait for the next daily cron; the full fix is a **scheduled `/api/jobs/drain` sweep on Vercel Pro** (already in S1) - then there is zero stranding. Until Pro + the flag, today's self-chain remains the active path.

---

## S7 — EXECUTED 2026-09-04 (prove it scales: deterministic invariant harness)

S7 has two halves: prove the LOGIC scales (done here, verifiable now, no infra/cost) and prove the INFRA scales (a load test against a deployed env, needs Vercel Pro - runbook below).

**Logic-scale harness (`scripts/check-scale.ts`, wired as `check:scale`, now 169/169 in the gate):** a deterministic simulation over the REAL shipped primitives - `readAllPages`, the durable queue + `drainQueue`, the AI budget thresholds, the rate-limit engine - at 1,500-account / 1,000-tenant scale, asserting:
1. **Paging (S0):** `readAllPages` returns ALL rows at 0/1/999/1000/1001/1500/4321 (no silent 1,000 cap), preserves order, and THROWS on a page error (never a silently-short read).
2. **Durable queue at 1,500 accounts (S2):** a full backlog of resumable, self-re-enqueueing `sync-account`-shaped jobs (4 slices each) ALL converge to complete; 3 poison accounts dead-letter after max attempts instead of looping; the hop cap holds; and after draining, a final claim returns nothing - proving the tail is not stranded and poison jobs are dead-lettered. Finished in 60 bounded rounds.
3. **Per-tenant AI budget (S4):** a whale over its $5 cap is paused for itself while global spend stays under $25, so AI stays UP for the other 900 tenants (the noisy-neighbour metric, proven).
4. **Rate limit (S3):** a 100-request burst on one key allows exactly 30 and limits 70 (the fixed-window cap engine that Upstash mirrors across instances).

This is a permanent regression gate: a future change that reintroduces a truncated read, a non-converging drain, a global-only budget, or a broken cap fails CI.

**Infra-scale load test (owner, post-Pro):** `scripts/loadtest.mjs` already exists (native fetch, bounded concurrency, latency percentiles, public read-only paths - safe by default). Run it against a PREVIEW/staging deploy, never prod:
`BASE=https://<preview-url> COUNT=500 CONCURRENCY=25 node scripts/loadtest.mjs`
Watch p95 vs the plan's targets (warm cockpit p95 < 1.5s), status distribution, and DB CPU in the Supabase dashboard. Do the whale/chaos cases (kill a drain mid-run; force a Meta 429 storm) once Pro + `SYNC_VIA_QUEUE=1` are on.

**Bottom line:** every code-side item in the ranked risk list (S0, S2, S3, S4) is now shipped AND proven to hold at 1,500-account scale by an automated gate. What remains for a real 1,000-DAU launch is owner-only: **Vercel Pro + Upstash (S1)**, flip `SYNC_VIA_QUEUE=1`, run the infra load test, and the business layer (S5: Stripe/deletion).

---

## S6 — EXECUTED 2026-09-04 (production RUM: measure real read-path speed)

The read path was already well-built for scale (SWR + shared L1/L2 `cockpit_cache` + rollups + 8s cold-pull cap + an instant `loading.tsx` skeleton), so S6's real gap was **measurement**: at 1,000 DAU you must be able to SEE whether loads are actually fast across many serverless instances, not guess. Added a lightweight, dependency-free Real-User-Monitoring pipeline.
- **Collector** (`components/app/vitals-reporter.tsx`, mounted in the /app layout): native `PerformanceObserver` + Navigation Timing capture **LCP, FCP, TTFB, CLS** and beacon each once on page-hide via `navigator.sendBeacon` - zero bundle deps, never competes with the page. (INP is left for the `web-vitals` lib later; the sink already accepts it - `ponytail:` ceiling noted.)
- **Sink** (`app/api/vitals/route.ts`): tiny, best-effort, write-only. Re-validates the metric with a pure module and RECOMPUTES the rating server-side (never trusts the client), attributes to the signed-in user when a session rides along, bounds the path. Public by design (a beacon may fire post-navigation) - allow-listed in `check:access-gate` as write-only/no-spend/RLS-default-deny.
- **Store** (`web_vitals`, migration 0041): RLS default-deny (service-role only), indexed by (metric, created_at); added to the account-deletion manifest (`EXPLICIT_DELETE_BY_USER`).
- **Report** (admin console): p75 per metric over the window, rated good/needs-improvement/poor (Google thresholds), so read-path speed is a visible trend.
- **Gate:** `check:vitals` (now 170/170) covers the pure rating + p75 (thresholds inclusive-at-good, nearest-rank, junk-value rejection).

**Verified:** `check:vitals` PASS; tsc 0; 170/170 gate green (fixed two real findings my change surfaced - the access-gate allowlist and the deletion manifest); build compiles.

**Owner note:** migration 0041 ships in the file set; it applies on the next deploy/migration run (the sink guards against the table being absent, so nothing errors pre-apply). p75 appears in the admin console once real loads have beaconed. INP field data needs the `web-vitals` package (deferred).

---

## S5 (deletion half) — EXECUTED 2026-09-04 (self-serve account deletion executor)

The non-payment half of S5, and a legally-required launch blocker (GDPR/CCPA "delete my account"). The manifest + coverage check already existed; this builds the EXECUTOR that acts on it, exactly per Rahul's 2026-09-01 decisions (self-serve, SOFT-delete with a 14-day grace, revoke Meta now, Cancel aborts). **Nothing is hard-deleted at request time** - a request only schedules the purge; the irreversible work runs in a cron after the grace, so a mis-click is always recoverable.
- **Plan is correct-by-construction:** `lib/account/purge-plan.ts` (pure) derives the ordered purge from the manifest - revoke externals, delete every `EXPLICIT_DELETE_BY_USER` table by user_id, anonymize the retained ones, then the auth-user delete LAST (cascades SET A). `check:account-purge` proves the plan covers every manifest table + revocation and that the irreversible auth-delete is strictly last with all revokes before it (171/171).
- **Executor** (`lib/account/deletion.ts`): `requestAccountDeletion` (soft-delete row + revoke Meta now, 14-day `purge_after`), `cancelAccountDeletion` (abort within grace), `getPendingDeletion` (for the UI), `purgeAccount` (runs the plan; `dryRun` returns the plan untouched), `purgeExpiredDeletions` (bounded cron body).
- **Surfaces:** `POST/DELETE /api/account/delete` (product-gated, own-user only - no one can schedule another tenant's deletion); `GET /api/cron/purge-deletions` (CRON_SECRET); a "Danger zone" card in Settings (two-step confirm; shows the purge date + Cancel when pending).
- **Store:** `account_deletions` (migration 0042, RLS default-deny), classified in the manifest (cascades with the auth user).

**Verified:** `check:account-purge` PASS; `check:account-deletion` still green (new table classified); tsc 0; 171/171 gate green; build compiles; scoring/golden untouched.

**Honest limits (owner):** (1) I did NOT run a real purge - it destroys data, and it needs a **throwaway-account live test** before the cron is enabled; the plan-coverage + order are proven offline, and the actual deletes mirror the same admin-client pattern used everywhere. (2) Migrations 0041 + 0042 apply on the next deploy (the code guards against the tables being absent). (3) The purge cron must be scheduled: add `/api/cron/purge-deletions` to `vercel.json` on Vercel Pro (Hobby's 2-cron limit is why it is a separate, owner-scheduled route); until then it can be run manually with the bearer. (4) "Re-login cancels" IS wired - in the login action (one write at sign-in only, no-op unless a pending row exists), so it never touches the page hot path; the Settings Cancel button also covers it.

**Still deferred (payment half of S5):** Stripe won't onboard early-stage Indian SaaS. Use **Razorpay** (INR customers) or **Paddle / Lemon Squeezy** (global, Merchant-of-Record, no US entity/Stripe needed). The token-allowance/metering layer is already provider-agnostic, so billing becomes "webhook -> set plan -> allowance" once a provider is chosen.
