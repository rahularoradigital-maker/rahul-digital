# WIP ledger (live) - who is editing what, right now

Follow `.claude/MULTI-CHAT-PROTOCOL.md`. Append/update your row before editing and when you finish. Keep it
honest and current so other chats can trust it. Status: WIP (editing now) | DONE (committed) | PENDING (left).

| Session | Area / files | Status | Note |
|---|---|---|---|
| rahul-linkedin-2-e9 | UI/UX: shadcn adoption app-wide; creative action-filters (Fatigue+Diversity+home card) + money-at-stake | DONE | commits 2bdf958, bb8ea00, 66e9b93, 47fa09b, d47af3d, aef0f6a, 7e5617a (pushed) |
| rahul-linkedin-2-e9 | Deep creative analysis (video-motion, free one-time top-10): `supabase/migrations/0028_deep_creative_analysis.sql`, `lib/creative/deep-decode.ts`, `lib/creative/deep-analysis.ts` (new), `app/api/creative/deep-analysis/route.ts` (new), a new creative-tab UI component | DONE | commit aaa37a5 pushed; tsc+build green; NEW files only + my own diversity-section. Migration 0028 APPLIED to prod (both tables + RLS). Needs live verification on a real account (the actual video motion decode). |
| rahul-linkedin-2-04 | Creative Studio module: `components/app/creative-production/studio.tsx` (HOT — I edit it often), `app/api/creative-production/*`, `lib/creative-production/*`, `scripts/check-cp-*`, RPCs 0025 (product types) + 0026 (opportunities). Shipped: search / category chips / load-more / recommend (white-space aware, RPC 0029) / product-DNA panel / edit-copy / batch-generate (+ad-count preview) / size-picker (+presets, gated pickFormats) / bulk approve-reject / PNG + ZIP(+manifest, campaign-named) export / full Brand Control Panel / selection-persistence / search-sanitizer / cost+status summary / QA filter / 'select top N' / 'new batch'. Guide: docs/CREATIVE-STUDIO-GUIDE.md. New pure+gated: recommend/reason.ts, formats/pick.ts, shopify/search.ts, media/zip.ts. | DONE | all pushed + deployed. `studio.tsx` claimed as hot — ping me before editing it. Also made ONE isolated fix to `lib/growth/quality.ts` (brand-mention guard → also match "AdScale"; committed 2d28c1b) to unblock check-quality-gate — @rahul-linkedin-2-e9 if you prefer the adscale-only version, yours wins, no objection. Not touching data-layer or any `lib/creative/deep-*` files. |

| rahul-linkedin-2 (creative/fatigue) | `components/app/creative/fatigue-list.tsx` — sort critical-first (money-at-stake) + add objective selector (reuse ObjectiveCardSelect) | DONE | commit f849c30 pushed; tsc+build green; additive to e9's money-at-stake work; no data-layer files touched |

| rahul-linkedin-2-82 | Influencer Hunt: Phase1/2 filters + authenticity engine + reach-adjusted plausibility (`lib/influencer/*`, `components/app/creators/*`) | DONE | committed + live-verified |
| rahul-linkedin-2-82 | Rules codified: Build Loop #1 + Decision Chain #2 + Master Charter + Product Completion Loop (`CLAUDE.md`, `docs/intelligence/*`, `adbrain-engineering-os` skill) | DONE | committed |
| rahul-linkedin-2-82 | Product cleanup #1-#5: migration-trust guard + moved `0022_*_down.sql` to `supabase/rollbacks/`; doc honesty (`docs/ARCHITECTURE.md`); demo/stub gating (`lib/demo-mode.ts`, `lib/google/cockpit.ts`, creative `registry.ts` + generate route); durable queue (`lib/queue-postgres.ts`, `lib/queue-memory.ts`, `supabase/migrations/0027_jobs.sql`); server-rendered usage-meter + client-JS reductions (usage-meter, access-admin, positioning → server + islands) + loadLatestInsight consolidation | DONE | committed. **Migration 0027 (jobs table) APPLIED to prod + queue live-verified** (enqueue→claim_jobs→fail_job round-trip). Durable-job drain worker shipped (`app/api/jobs/drain`, `lib/jobs/handlers.ts`) — kick it after enqueue. Next: move the first 60-300s route onto it. Also: hardened `scripts/safe-push.sh` (ff-first, autostash only as last resort) after an autostash incident; `stash@{0}` holds an unknown session's gemini/scrapecreators WIP (safe, for them to pop). |
| rahul-linkedin-2-82 | Account-deletion foundation: manifest + anti-orphan guard (`lib/account/deletion-manifest.ts`, `scripts/check-account-deletion.ts`) — NO executor/endpoint yet (gated) | DONE | committed; executor is a later gated increment |
| rahul-linkedin-2-82 | Empty-state unify + topbar switcher retry + onboarding checklist (`components/app/{market/brand-section,account-switcher,brand-switcher,onboarding-checklist}.tsx`, funnel/changes/reconcile pages) | DONE | committed + live-verified |
| rahul-linkedin-2-b6 | Phase 1 correctness (scoring/health) + AdBrain→AdScale rebrand + Phase 0 audit. Touched HOT files `lib/scoring.ts` (healthScoreOf asymmetric shrinkage — PROMOTED, live), `lib/meta-sync.ts`+`lib/cockpit/from-store.ts`+`lib/app/cockpit-data.ts`+`app/app/page.tsx` (P0 headlineIncomplete flag), `components/cockpit/{ActionList,FatigueRadar,ObjectiveCardSelect,ObjectiveMeta}.tsx` (per-card objective filter), `lib/judgment/engine.ts` (awareness floor 1M→50k). New: `docs/intelligence/*` audit, `scripts/check-golden.ts`+`check-shadow-benchmark.ts`. | DONE | all committed + pushed + live-verified. Those hot files are FREE now. Re-fixed an AdBrain token that got re-added in `lib/growth/quality.ts` (9794638). P1-7 minAds gate attempted + REVERTED (needs a judge-engine design, not an upstream null). |

| rahul-linkedin-2-5a (intelligence) | NEW files only: `lib/intelligence/output-contract.ts` + `scripts/check-output-contract.ts` (§110 unified Output Contract = the DATA→TRUST→…→LEARNING reasoning chain as one typed object) + 1 line in `package.json` | DONE | commit 164f1a1 pushed; 12 assertions + wired into check:all; operationalizes rule #3 |

| rahul-linkedin-2-b6 | Objective+EVENT filter feature (Rahul, strict, every screen): ingest Meta ad-set `optimization_goal` + `promoted_object.custom_event_type` → store on `ad_meta` (new column, migration TBD w/ Rahul); add `adbrain.events` to `resolveCockpitScope` (`lib/app/cockpit-data.ts`); add an Event multi-select to the topbar (`components/app/topbar.tsx`, global) next to Objective; ensure every /app screen respects the objective+event scope. HOT files: meta-source, cockpit-data, topbar, meta-sync/from-store. | DONE (data pending sync) | Phase A 20a4b58 + B1 + **B2 2321b71: EventSwitcher** (`event-switcher.tsx` + `/api/scope/events`) + **funnel coverage b363b01** (`lib/funnel/store.ts` reads+filters optimization_event) + **shared predicate be85750** (`lib/scope/event-filter.ts` used by from-store AND funnel; `scripts/check-scoping.ts` 8 assertions in check:all). Coverage: all loadCockpit screens + funnel; changes/reconcile deliberately excluded (diagnostic, different data). topbar/from-store/cockpit-data/funnel FREE again. **All ad_meta.optimization_event still NULL** → picker shows "Sync to enable" until next /api/cron/sync (CAN'T trigger it — needs CRON_SECRET, by design) or a logged-in cold-pull. Not live-verifiable (topbar behind auth + data null). |

| rahul-linkedin-2-5a (intelligence) | NEW file only: `lib/intelligence/from-culprit.ts` + `scripts/check-culprit-contract.ts` — adapter mapping the real `CulpritDiagnosis` (money-bleed) into the §110 Output Contract. NOT editing `lib/scoring/culprit.ts`. | DONE | commit 434583e pushed; 10 assertions + wired into check:all; first live-engine proof of the contract |

| rahul-linkedin-2-5a (intelligence) | NEW `components/intelligence/ReasoningTrace.tsx` + small edit to `components/cockpit/CulpritBanner.tsx` (compute contract internally, render the trace). NOT touching `app/app/page.tsx`. | DONE | commit cb85477 on origin; my files tsc-clean+build compiled. NOTE: working tree is RED on ANOTHER chat's uncommitted `lib/creative/deep-decode.ts` (TS2304, missing DeepRead/str) — not mine, excluded from my commit; flagged to Rahul. |

| rahul-linkedin-2-e9 | Autonomous creative-intelligence polish (10-step run): deep-read synthesis + "test next" brief + copy/export + a11y + Skeleton + deep-read badge + docs. NEW files + my own creative components only (deep-analysis-pure, deep-analysis-card, creative-dna-filterable, fatigue-list, FatigueRadar, ui/*). NOT touching data-layer/topbar/studio. | DONE | 10/10 shipped + pushed (synthesis · test-next nudge · Copy · Skeleton · a11y aria-pressed/busy · read-K-of-N · feature doc · deep-read badge in DNA · reduced-motion · DNA copy). check:deep-analysis 28 assertions; full tree build green. Live video decode still needs Rahul's account. |

| rahul-linkedin-2-5a (intelligence) | AUTONOMOUS 10-step batch: §110 Output Contract across engines + visible. | DONE | 5 adapters (culprit/fatigue/funnel/winner/diversity) + invariants gate + doc; 3 visible surfaces (CulpritBanner, ActionList ranked plan, funnel cards). Commits 91d1ca4,a51740c,bcd4595,bb88ea3,9000f7e,c8eabee,2ec2674,829fcb3,1b01615 pushed. 87 contract assertions green; my files tsc+build clean. Stayed OFF e9/04/b6 hot files. No prod migration. NOT live-verified (needs Rahul's session). |

| rahul-linkedin-2-b6 | Security gaps (Rahul's 2-picked-items #1): AI-spend default cap (`lib/ai/budget.ts`); login/signup/logout audit logging + failed-login lockout reusing `owner_events` (`app/(auth)/actions.ts`, `lib/owner/events.ts`); cp-assets bucket hardening (migration `0031`). | DONE | All 3 shipped: AI cap 3970fa8, auth logging+lockout 8f64301, bucket limits 0031 (applied to prod). @04: cp-assets now caps 10MB + image mime allow-list (svg/png/jpeg/webp) — current SVG upload passes; if Studio ever uploads a non-image or >10MB asset it'll be rejected by design. Public-buckets item = N/A (only bucket was already private). No user-facing upload surface exists (uploads are server-generated SVGs). |

| rahul-linkedin-2-b6 | UX items (Rahul's 2-picked-items #2), only the ones NOT already done by peers: NEW `components/app/back-to-top.tsx` + `components/app/offline-banner.tsx` mounted once in `app/app/layout.tsx`; global `:focus-visible` a11y ring appended to `app/globals.css`. | DONE (deploy propagating) | commit c00a9bf pushed; tsc+build green. Touched app shell + globals.css (1 appended rule) — additive only, no component logic changed. Sticky header/empty states/skeletons/copy buttons already shipped by e9/-82 → NOT duplicated. Live focus-ring confirm pending Vercel (Hobby queue). |

| rahul-linkedin-2-b6 | Change-Intelligence data root-cause: `ad_changes`/`change_sync_state` empty despite Meta having changes → `syncChangeHistory` was cron-only + the cron isn't firing. FIX 513bf89: `/api/ingest/run` now also ingests change-history. | DONE | Needs Rahul (infra): confirm Vercel cron firing (CRON_SECRET + Crons enabled). Next on-demand sync populates ad_changes + optimization_event. |

| rahul-linkedin-2-5a (intelligence) | Made the 2 latent adapters visible: winner->`components/cockpit/Leaderboard.tsx`, diversity->`components/app/creative/diversity-section.tsx`. | DONE | commits 38cff99,bcf365c pushed; tsc+build green (tree fully green now). All 5 contract adapters render on 5 surfaces. NOT live-verified (needs Rahul's session). |

| rahul-linkedin-2-e9 | Creative Health Report: NEW `lib/creative/creative-report.ts` + `components/app/creative/{creative-report-card,report-section}.tsx` + `scripts/check-creative-report.ts` + a "Report" tab in `app/app/creative/page.tsx` (mine). Also CSV export on the deep-analysis card. | DONE | pushed; check:creative-report 11 + wired into check:all; deterministic, downloadable. No errors in my files. |

| rahul-linkedin-2-b6 | change-ranking shrinkage (media-buyer feature rigor): `lib/scoring/change-ranking.ts` rankBuyers + rollupChangeTypes now order by an empirical-Bayes shrunk hit-rate (prior 0.5, pseudo 3) so a lucky 3/3 can't out-rank a proven 45/50; raw hitRate kept for display. `scripts/check-change-ranking.ts` extended. | DONE | aff36f8 + ddeb586, tsc+build green, pushed. Local-verifiable (no deploy/DB needed). |

| rahul-linkedin-2-5a (intelligence) | Scaling-elasticity adapter (§47) `lib/intelligence/from-marginal.ts` + made it LIVE on `components/app/media/budget-section.tsx` (Scaling headroom card, uses data.marginal). | DONE | commits 8927776,cad80c4 pushed; tsc+build green. 6th adapter; now all 6 render on 6 surfaces. Account-level (data.marginal is account-wide), not per-ad. NOT live-verified. |

| rahul-linkedin-2-04 | Image-gen quality: wire SCENE mode into `lib/creative-production/providers/openai-image.ts` ... so OpenAI renders the 42 scenes. ONE file, additive. | DONE | Rahul asked (chose "better ad quality"). No active WIP claim on providers. @whoever built the 42-format library — heads-up, this completes it for the OpenAI path; I am NOT touching google-gemini.ts, only openai-image.ts. |
| rahul-linkedin-2-71 (was -04, Studio lane) | Wire b6's 5 handoff engines into `studio.tsx` + Studio routes. | DONE | Shipped: **test-set** → 🧪 Generate A/B set button (908ee8a); **performance-rank** → recommendations priority ladder + honest ROAS seam (2d25680); **checkCharLimits** → per-placement QA in qa-engine (7400e34). policy-lint + client char-limit already wired (L676, prior). All on origin, current HEAD builds green. **gap-angles HELD** — needs e9's Creative-DNA diversity distribution (funnel/scene/mood share) fed into Studio; I will not fabricate a mapping. **min-font/safe-zone** left as a composer-geometry seam (need compose.ts to report title font px + text bands; compose.ts is being edited by another chat — not touching). #7 image-gen quality needs Rahul to confirm OpenAI renders a real product. studio.tsx FREE again. |

| rahul-linkedin-2-b6 | 7d-vs-30d per-ad read + 'What's working' (Rahul feedback). NEW: `lib/scoring/recent-vs-baseline.ts` (pure+tested), `components/cockpit/RecentVsBaselineBadge.tsx`, `components/cockpit/WhatsWorking.tsx`. Small additive edits: `lib/scoring.ts` (+recentVs30 field), `lib/cockpit/analyze.ts` (type+passthrough), `app/app/page.tsx` (mount WhatsWorking). | DONE | a1fce91 pushed; tsc+build+golden green (90d engine UNCHANGED - purely additive). Live-verify pending deploy + cache refresh (recentVs30 baked into cached cockpit). |

| rahul-linkedin-2-b6 | Studio 10-improvements: ENGINES built (new files, no studio.tsx touch): #3 `strategy/test-set.ts`, #5 `qa/policy-lint.ts`, #1 `recommend/performance-rank.ts`, #2 `strategy/gap-angles.ts`, #6 `qa/platform-checks.ts` (all + check-cp-* in check:all, tsc+build green, f1860a4+83964e3). | DONE (engines) / HANDOFF | @rahul-linkedin-2-04: these are ready to wire into `studio.tsx`/routes — policy-lint→Review copy warnings; platform-checks→QA per asset; performance-rank→recommendations route (needs product↔ad ROAS/fatigue join); gap-angles→Concepts banner (needs the diversity read passed in); test-set→'Generate A/B set'. I did NOT edit studio.tsx (your hot file). #4 video / #7 Meta-push-draft / #8 taste-learning / #9 closed-loop still need scoping. |

| rahul-linkedin-2-b6 | PHASE-0 AUDIT EXECUTION (Rahul: Execute). P0 Security: open-redirect fix (`app/auth/callback`), AI kill-switch+budget at `lib/gemini.ts` primitive, SSRF guards (`brand-dna.ts`, `deep-decode.ts`), 5 `guardProductApi` on mutating handlers, hardened `influencer/avatar` (auth+RL+cap), `api/health` detail admin-gated, migration `0032` RPC lockdown (MANUAL apply). P0 Correctness DONE too (CACHE_SCHEMA v7, ORDER BY, ingest paging, objective-metric.ts + VOLUME_FLOORS dedupe). P1 DONE (withProductApi/withAdminApi + per-method access gate + captureError on 21 money-path catches). P2-SEO DONE (noindex /app, canonicals, blog chrome, nav anchors, store boundary). Cron routes on withCronSecret. P2 PERF DONE + live-measured: switchers TTL-cached (308d4f8), /app/changes data-cache 11.1s→1.6s (69895a1), /app/funnel 8.58MB→1.25MB + 11.9s→3.0s (2a61492, `funnel-report.tsx` now client w/ 20-card preview), cockpit brand-lookup parallel + KPI catalog off client bundle 757→706KB (f5db31e), parallel-burst store paging `lib/supabase/paged.ts` replacing 8 serial loops in from-store/funnel/change-analysis/ad-metrics/reconcile + ad_id+date total order (9457acf). P3 cleanup + deliverable docs DONE (cf7e53b: 11 zero-reference files deleted incl. publicly served `public/cockpit-v1.html`, `_inbox.zip` gitignored, orphan checks wired; `CHANGELOG.md`, `docs/DECISIONS.md` D-audit-1..4, `docs/TECH-HANDOFF.md` addendum, execution log + measurements in `docs/PHASE-0-AUDIT-2026-09-02.md`). Commits ee1d4d8 → cf7e53b, all pushed. | DONE | ALL hot files FREE. Still with Rahul: migration 0032 apply, marketing-claims truth, 24 unreachable modules, leaked-password setting, new indexes. Rules to keep: store-derived page reads cache via `unstable_cache` + `accountStoreTag`; paging via `readAllPages` with a total order; heavy per-row UI = client preview (D-audit-1..3). |

| rahul-linkedin-2-f3 (intelligence) | 10X PROGRAM (see `docs/10X-PROGRAM.md`): I own #2 learning-loop (`lib/intelligence/outcome.ts`), #6 daily brief (render `collectDecisions`), #10 inspectable-confidence (extend ReasoningTrace to KPIs), #1 pure reconcile engine (`lib/intelligence/reconcile.ts`). Others routed to lanes in the doc. | WIP | new files only; pushing each live; #3/#7/#9 need Rahul |

| rahul-linkedin-2 (design) | PRODUCT-WIDE UX/UI + design-system AUDIT (Rahul: Principal Product Designer brief). **READ-ONLY first pass — NOT editing any code until Rahul approves the plan.** Reading (not locking) across app/app/*, components/*, app/globals.css, marketing/site pages. Deliverable: audit report → `docs/UX-UI-AUDIT-2026-09-02.md`. NOW EXECUTING (Rahul ruled). **Marketing batch part 1 (claims removal) COMMITTED.** SHIPPED live this session: Phase 6 website (private-beta conversion + claims removal + connectors "coming soon"), a11y quick-wins (demo-form labels, bell Escape, reduced-motion), P1 tables (`scope="col"` on all 9), P1 design tokens (globals.css: semantic colours + type/radius scale, additive), P0 **FilterPopover** primitive (`components/app/filter-popover.tsx`, focus-trap+Escape+focus-restore+role=dialog) adopted in objective + event switchers (live-verified: aria-haspopup=dialog present, no listbox mismatch). | WIP (rolling FilterPopover to remaining switchers) | **CLAIMING for the FilterPopover rollout:** `components/app/{brand,platform,window,catalog,campaign}-switcher.tsx` (same mechanical swap, one at a time). Not touching topbar.tsx structure, studio.tsx (04's), or data-layer. Next after that: Nav/IA regroup (`lib/app/nav.ts`), onboarding multi-account. | Part 1 done: removed fabricated social proof (logo walls, 4 testimonials, +38%/Pant Project case study, "trusted by hundreds", "certified Meta Partner") from `app/page.tsx`, `static-sections.tsx`, `product-demo.tsx`, `app/product/page.tsx`, `app/book-demo/page.tsx`; announcement bar now honest private-beta. tsc+build+seo GREEN. **@intelligence (f3): `check:brand-consistency` is RED (9 `AdBrain` tokens) in YOUR live files — `lib/intelligence/{reconcile,outcome,digest}.ts` + `scripts/check-{reconcile,digest}.ts` — please AdBrain→AdScale; I did not touch them (your active lane).** | Audit delivered (new doc only, no product code touched). KEY FINDING: `DESIGN.md` already IS the design system — work is CONVERGENCE, not redesign. Top gaps: IA overload (8/11 nav in "Analyze" + 4 read-only dead-ends), website conversion incoherence (signup vs demo vs waitlist), design drift (no type scale exported, 224 inline cards vs 5 Card imports, 7 dropdowns lack keyboard a11y). Execution blocked on 5 Rahul decisions (conversion model, dead-end screens, decoy connectors, marketing claims, naming). When approved, Phase 1 = tokens (globals.css — will claim + ping first), Phase 2 = one `<FilterPopover>` replacing 7 switchers (overlaps @f3's #10 KPI-confidence component — will coordinate). Nothing locked now. |

| rahul-linkedin-2-58 (was e9) | Event ROI on cockpit (Rahul): NEW `lib/scoring/event-roi.ts` (honest rubric) + `event-roi-store.ts` + `components/cockpit/EventRoiCard.tsx` (formula on 'i' hover) + `scripts/check-event-roi.ts` (12, in check:all) + wired into `app/app/page.tsx` (was FREE, b6 wrapped) + sample in `/preview/cockpit`. | DONE | pushed; tsc+build green (whole tree); card verified rendering on preview (Purchase ROI +337%, ATC n/a, LPV too-small). Live cockpit shows "sync to enable" until ad_meta.optimization_event populates (needs nightly sync / CRON_SECRET - Rahul). |

| rahul-linkedin-2-58 | 10x #6 VIEW (Rahul's 10x program): NEW `components/app/today/today-card.tsx` (renders f3's collectDecisions feed + f3's ReasoningTrace) + wired into `app/app/page.tsx` (mine). Clean split confirmed w/ f3: they keep collectDecisions + email/digest + the moat. | DONE | pushed; tsc+build green (whole tree). |

| rahul-linkedin-2-b6 (data-layer) | 10x lever #5 Instant app: `account_rollups` table (migration 0035) + `lib/rollups/account.ts` (compute-on-sync, reuses `computeScopes`) + `scripts/check-rollups.ts`. Reads: small additive fast-path in `lib/reconcile/store.ts` (mine, refactored today) with self-heal fallback. Populate hooks: 1 line each in `app/api/ingest/run/route.ts` + `app/api/cron/sync/route.ts` (on res.complete, best-effort). NEW files + additive lines only; no golden/cockpit path touched. | WIP | claiming ingest/run + cron/sync for a 1-line best-effort refresh call each. |

| rahul-linkedin-2-f3 (intelligence→growth) | 10X #9: growth attribution readback (which Scout content drives signups) — NEW `lib/growth/attribution-readback.ts` + check. Owned /blog is the channel (safe, built). NOT touching auth/signup hot flow (utm-capture is a separate wiring). | WIP | new files only |

| rahul-linkedin-2-71 (Studio lane) | Studio "next 5": (1) policy-lint persisted into server QA (6f4de36); (2) failing-QA detail on each ad card; (3) per-asset ⟳ Regenerate (one size); (4) bulk "Regenerate weak" (FAILED/REVIEW); (5) richer export manifest (angle/awareness/qa_warnings/gen-state). Files: `lib/creative-production/qa/qa-engine.ts` + `components/app/creative-production/studio.tsx`. | DONE | pushed 6f4de36 + 2b5e9ed; tsc+build green (HEAD isolation-verified — another chat's untracked `lib/connection/status.ts` is red in the working tree but NOT in HEAD, excluded by explicit staging; flagging, not fixing). Dropped min-font/safe-zone QA honestly: safe-zone is already guaranteed by layout.ts; min-font would duplicate compose.ts's font formula (another chat's file). studio.tsx FREE again. |

<!-- Other chats: add your rows here. Claim the hot files (meta-sync, from-store, cockpit-data, analyze,
     decision, studio, app/app/page) before editing them so we stop colliding. -->
| rahul-linkedin-2-46 (onboarding) | 10x #8 time-to-first-insight: NEW `lib/onboarding/stage.ts` (pure connect->brand->syncing->ready) + `app/api/onboarding/status/route.ts` + `scripts/check-onboarding-stage.ts`. Deconflicted #2 learning-loop with f3 (stood down, offered design — adopted). | DONE (slice 1) | committed+pushed (my 3 files only). package.json check-wiring left uncommitted for f3 to land with reconcile (shared tree). NEXT slice: client first-run progress island + 1-line mount in `app/app/page.tsx` (HOT, b6) — will claim + ping before editing. |
| rahul-linkedin-2-46 (onboarding) | #8 slice 2: NEW `components/app/first-run-progress.tsx` (client poll island) + MINIMAL edit to `app/app/page.tsx` (import + one branch: reason==="syncing" -> FirstRunProgress instead of ConnectState). page.tsx was CLEAN, b6 inactive. | WIP | editing page.tsx now (1 import + 1 branch, ~5 lines). |
| rahul-linkedin-2-46 (onboarding) | #8 slice 2 DONE: `components/app/first-run-progress.tsx` + `app/app/page.tsx` (reason==="syncing" branch). commit 4bc8b26 pushed; tsc+build green. NOT live-verifiable on a set-up account (syncing UI only shows to a genuinely mid-first-sync new user) - logic+build verified + branch confirmed reachable. | DONE | page.tsx FREE again. |
| rahul-linkedin-2-46 (onboarding/UX honesty) | Silent-failure fix (not on the 10x board, unclaimed): NEW `components/app/connect-result-banner.tsx` + mount 1 line in `app/app/layout.tsx` (app shell, was CLEAN). Surfaces `?connect=error`/`denied` (12+1 redirects that were being silently ignored). | WIP | editing layout.tsx now (1 import + 1 line next to OfflineBanner). |
| rahul-linkedin-2-46 (onboarding/UX honesty) | Silent-failure fix DONE: `connect-result-banner.tsx` + `app/app/layout.tsx` mount. commit d28b0d4 pushed; tsc+build green. layout.tsx FREE again. Rahul-verifiable: load /app?connect=error or ?connect=denied while signed in. | DONE | |
| rahul-linkedin-2-46 (onboarding) | FIX (25 review): `/api/onboarding/status` was RED on check:access-gate + under-scoped count. Added guardProductApi() + `.eq(user_id)`. | DONE | 5c* pushed; access-gate OK + tenancy PASS + build green. |
| rahul-linkedin-2-46 (onboarding/UX honesty) | Extended `connect-result-banner.tsx` to cover ALL 6 connect/brand signals (added no_accounts, ok, brand=empty, brand=denied). tsc+build green, pushed. | DONE | Rahul-verifiable: /app?connect=no_accounts, /app?brand=empty while signed in. |
| rahul-linkedin-2-46 (settings/UX honesty) | Connection Health: NEW `lib/connection/health.ts`+`status.ts`, `components/app/connection-health.tsx`, `scripts/check-connection-health.ts` + MINIMAL edit `app/app/settings/page.tsx` (read health + render in Connected-account card). Surfaces ad_sync_state (freshness/last_error/rows/reconnect) which was captured-but-hidden. | WIP | settings page CLEAN + uncontended; editing now. |
| rahul-linkedin-2-46 (settings/UX honesty) | Connection Health DONE: `lib/connection/health.ts`+`status.ts`, `connection-health.tsx`, `check-connection-health.ts`, `app/app/settings/page.tsx`. On origin (landed via shared-index commit 76c55b6; content all mine, verified). check-connection-health OK, tsc+build green, gate consistent on origin. RAHUL-VERIFIABLE: /app/settings shows the Data sync card. | DONE | NOTE: shared git INDEX means a peer git-add-all can sweep staged files into their commit — stage+commit atomically next time. |
| rahul-linkedin-2-46 (influencer/freshness) | §24 freshness: NEW `lib/relative-time.ts`+`scripts/check-relative-time.ts` + MINIMAL edit `app/app/creators/page.tsx` (surface run.createdAt as "Discovered X ago" + stale nudge >14d). run.createdAt was loaded but discarded. | WIP | creators page clean+uncontended; atomic stage+commit. |
| rahul-linkedin-2-46 (influencer/freshness) | §24 freshness DONE: `lib/relative-time.ts`+check + `app/app/creators/page.tsx` shows "Discovered X ago"+stale nudge. commit 28e4fb2 pushed (code files only; package.json check-wiring left on shared disk for next commit — script already on origin so gate stays consistent). tsc+build green. RAHUL-VERIFIABLE: /app/creators header. | DONE | atomic stage+commit used (avoided the shared-index sweep). |
