import HomeRedesign from "@/components/marketing/home-redesign";

// Kept as a staging alias of the redesign (same component now powering the live homepage), so old
// review links still resolve.
export default function Page() {
  return <HomeRedesign />;
}
